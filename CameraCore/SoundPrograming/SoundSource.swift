//
//  SoundPrograming.swift
//  MystaVideoModule
//
//  Created by 町田 秀行 on 2018/03/06.
//  Copyright © 2018年 tv.mysta. All rights reserved.
//

import Foundation
import OpenAL

/*
final class SoundSource {
    private var buffer: ALuint
    private var source: ALuint
    private let fullFilePath: String
    
    init?(fullFilePath: String) {
        /*
        let buffer = alureCreateBufferFromFile(fullFilePath)
        
        if buffer == alNone {
            print("Failed to load \(fullFilePath)")
            return nil
        }
        
        var source: ALuint = 0
        alGenSources(1, &source)
        
        alSourcei(source, AL_BUFFER, ALint(buffer))
        
        self.buffer = buffer
        self.source = source
        self.fullFilePath = fullFilePath
         */
    }
}
*/
